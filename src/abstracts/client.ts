import { Entity } from "./generic-entity";

/**
 * This client is generic and not associated with a specific entity but rather provides support for a platform feature area.
 * A client is a Nblocks Block feature area
 */
export abstract class Client extends Entity {}